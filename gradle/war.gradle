apply plugin: "war"

bootWar {
    mainClassName = "me.rasztabiga.fridgy.products.ProductsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
